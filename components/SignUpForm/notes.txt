//1 enter phone number && accept terms of use
    // valid -> next
    // !valid -> ask to fix

//2 enter sms code
    // match -> freeze + next
    // !match -> ask to re-enter
       


//3 set password && repeat password
    // match -> freeze + next
    // !match -> ask to fix

//4 enter email
    // email format valid -> freeze + next
    // ask to fix

//5 choose account type
    // freeze + next

//6 personal details: name, lastname, patronymic, dob, country
